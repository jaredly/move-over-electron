(executable
  (public_name HelloDev)
  (libraries Hello)
  (modules HelloDev)
  (modes native)
  (preprocess (pps belt.ppx fluid.ppx)))

(executable
  (public_name HelloProd)
  (modules HelloProd)
  (libraries Hello)
  (modes native)
  (preprocess (pps belt.ppx fluid.ppx)))