(executable
  (public_name HelloDev)
  (libraries Hello dynlink)
  (modules HelloDev)
  ; (modes native)
  (link_flags -cclib -framework -cclib Cocoa -cclib -framework -cclib Carbon -cclib -framework -cclib CoreImage)
  (preprocess (pps belt.ppx fluid.ppx)))

; (executable
;   (public_name HelloProd)
;   (modules HelloProd)
;   (libraries Hello)
;   (modes byte)
;   (link_flags -cclib -framework -cclib Cocoa -cclib -framework -cclib Carbon -cclib -framework -cclib CoreImage)
;   (preprocess (pps belt.ppx fluid.ppx)))